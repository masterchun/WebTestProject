<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.main-mapper">
  <select id="mainCampingData" resultType="CampingVO">
    SELECT image, name, likecount, jjimcount, price_min, rownum
    FROM (SELECT image, name, likecount, jjimcount, price_min
    FROM camping)
    WHERE rownum &lt;= 8
  </select>
</mapper>