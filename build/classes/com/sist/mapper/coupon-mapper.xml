<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.coupon-mapper">
  <select id="couponListData" resultType="CampingVO">
    SELECT id, name, image, telephone, locality, num
    FROM (SELECT id, name, image, telephone, locality, rownum as num
    FROM (SELECT id, name, image, telephone, locality
    FROM camping ORDER BY id ASC))
    WHERE num BETWEEN #{start} AND #{end}
  </select>
  <select id="couponTotalPage" resultType="int">
    SELECT CEIL(COUNT(*) / 12.0) FROM camping
  </select>
</mapper>